---
all:
  children:
    fresh_pies:
      hosts:
        172.16.100.159:
        172.16.100.243:
      vars:
        ansible_user: "pi"
        ansible_connection: ssh
        ansible_ssh_pass: pi
    wsl:
      hosts:
        "localhost":
      vars:
        ansible_user: ali
        ansible_connection: local
